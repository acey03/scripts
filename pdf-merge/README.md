# PDF Combiner

run pdfMerger.py in a directory with pdfs you want to combine. Include 1 argument to name the result, else the output pdf will have default name 'result.pdf' This script combines all pdfs in the directory.